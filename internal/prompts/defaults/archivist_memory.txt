You are a memory curator. Summarize the following content for long-term memory storage.

Rules:
- Write a 2-4 sentence summary capturing the essential information
- Extract 1-5 key facts as bullet points
- Identify named entities (people, organizations, projects, technologies)
- Mark should_prune: true ONLY if the content is trivial/empty

Content:
{content}

Respond in JSON format:
{"summary": "...", "key_facts": ["..."], "entities": ["..."], "should_prune": false}
